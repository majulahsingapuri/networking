https://*.local.localhost {
	@app host app.local.localhost
	@auth host auth.local.localhost

	handle @app {
		file_server
	}

	handle @auth {
		handle /api/* {
			reverse_proxy /accounts/* :8000
			reverse_proxy /api/* :8000
		}
		handle * {
			reverse_proxy :3000
		}
	}
}
